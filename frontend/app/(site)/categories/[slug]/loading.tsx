import { LandingSkeleton } from '@/components/discovery/skeletons';

/** Low-fidelity stand-in shaped like the real page — see skeletons.tsx. */
export default function Loading() {
  return <LandingSkeleton />;
}
