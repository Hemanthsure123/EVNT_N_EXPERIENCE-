import * as React from 'react';
import { LoginStep } from '@/components/booking/step-login';

/** Step 2 — sign in, and only when there is no session. */
export default function BookingLoginPage() {
  return <LoginStep />;
}
