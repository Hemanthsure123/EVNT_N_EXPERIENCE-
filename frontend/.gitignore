# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
# Verification builds. `next build` into the default dir while `next start`
# is serving it corrupts the running server on Windows, so a check build
# goes to NEXT_DIST_DIR=.next-verify instead.
/.next-verify/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/playwright-report
/test-results
/blob-report
/playwright/.cache

# storybook
/storybook-static

# misc
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (never commit real values)
.env
.env*.local

# typescript
*.tsbuildinfo
